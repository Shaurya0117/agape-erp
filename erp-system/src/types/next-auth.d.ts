import NextAuth, { DefaultSession } from "next-auth"; declare module "next-auth" { interface Session { user: { id: string; isSuperAdmin: boolean; canManageFinancials: boolean; canManageProjects: boolean; canManageLogistics: boolean; canManageUsers: boolean; } & DefaultSession["user"] } interface User { isSuperAdmin: boolean; canManageFinancials: boolean; canManageProjects: boolean; canManageLogistics: boolean; canManageUsers: boolean; } } declare module "next-auth/jwt" { interface JWT { id: string; isSuperAdmin: boolean; canManageFinancials: boolean; canManageProjects: boolean; canManageLogistics: boolean; canManageUsers: boolean; } }
